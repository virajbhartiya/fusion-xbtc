{
  "name": "fusion-xbtc-eth-contracts",
  "version": "1.0.0",
  "description": "Ethereum HTLC contracts for Fusion XBTC",
  "main": "contracts/ETHHTLC.sol",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:fusion": "hardhat run scripts/deploy-fusion-htlc.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@types/node": "^20.10.0",
    "hardhat": "^2.26.1",
    "typescript": "^5.8.3"
  }
}
